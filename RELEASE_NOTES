## 1.3.2

- 版本号改为 1.3.2、1.3.3，不再使用 1.3.1-N
- 每个版本同时提供软件包与整包固件
- 修复软件包安装失败（校验与 ipk 格式）
- 更新页可检查、下载并升级固件，默认保留配置后重启
- 「已是最新」时禁用更新软件包 / 更新固件按钮
- 下载时显示进度条
- 连接页状态只显示账号、线路、出口
- 连接互斥：切换线路不再用 pending 覆盖正在进行的 connect
- 连接过程中不再反复重启 dnsmasq
- 重连达到上限后停止死循环
- 防火墙仅在首次把 VPN 加入 wan zone 时 reload
