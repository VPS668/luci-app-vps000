## 1.3.1

- GitHub Release 必须包含 ipk 与 MT7628 sysupgrade 固件
- 更新页可检查、下载（含镜像）并升级固件，默认保留配置后重启
- 「已是最新」时禁用更新软件包 / 更新固件按钮
- 下载时显示进度条
- 连接互斥：切换线路不再用 pending 覆盖正在进行的 connect
- 连接过程中不再反复重启 dnsmasq
- 重连达到上限后停止死循环
- 防火墙仅在首次把 VPN 加入 wan zone 时 reload
